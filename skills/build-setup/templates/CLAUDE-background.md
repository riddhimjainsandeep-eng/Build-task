# Background and provenance

Why things in `CLAUDE.md` are the way they are. `CLAUDE.md` is charged on every
turn of every session; this file is read once, when a decision there looks
arbitrary or when someone is about to re-propose something already rejected.

## Decisions and why

<One heading per decision. What was chosen, what was rejected, and the reason —
especially the reasons that are not obvious from the code.>

## Things tried and rejected

<So they are not re-proposed. Include what would have to be different for the
answer to change.>

## Resolved history

<Superseded facts, kept rather than deleted:
`~~the old claim~~ **RESOLVED <date> — what changed.**`>

## Deferred and known dead code

<Things deliberately left undone or left in place, so they are not rediscovered
as bugs.>
